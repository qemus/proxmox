services:
  proxmox:
    hostname: proxmox
    image: dockurr/proxmox
    container_name: proxmox
    environment:
      USERNAME: "root"
      PASSWORD: "root"
    ports:
      - 8006:8006
    restart: always
    privileged: true
    stop_grace_period: 1m
